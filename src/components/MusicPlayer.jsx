// Placeholder — full Spotify embed is implemented in Task 7.
export default function MusicPlayer() {
  return (
    <div
      className="fixed bottom-0 left-0 right-0 bg-gradient-to-r from-pink to-teal text-white p-3 text-center"
      style={{ paddingBottom: 'calc(0.75rem + env(safe-area-inset-bottom))' }}
    >
      🎵 Music coming soon
    </div>
  )
}
